<div class="my-modal-overlay" id="quickViewModal">
  <div class="my-modal-container">
    <div class="modal-close" onclick="closeQuickView()">
      {% render "close-icon" %}
    </div>
    <div class="my-modal-content">
      <!-- Product data will be dynamically inserted here -->
    </div>
  </div>
</div>



<style>
  /* Modal Overlay */
  .my-modal-overlay {
    background: rgba(0, 0, 0, 0.7);
    position: fixed;
    width: 100%;
    height: 100%;
    top: 0;
    left: 0;
    display: none;
    z-index: 99999;
    justify-content: center;
    align-items: center;
    padding: 1rem;
  }

  .my-modal-overlay.active {
    display: flex;
  }

  /* Modal Container */
  .my-modal-container {
    background: #fff;
    padding: 3rem;
    max-width: 400px;
    width: 100%;
    border-radius: 8px;
    text-align: center;
    position: relative;
  }

  /* Close Button */
  .modal-close {
    width: 100%;
    text-align: right;
    position: absolute;
    top: 10px;
    right: 15px;
  }

  .modal-close svg {
    width: 16px;
    height: 16px;
  }

  .modal-close svg:hover {
    cursor: pointer;
  }
</style>
